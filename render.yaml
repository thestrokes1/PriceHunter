services:
  - type: web
    name: pricehunter-api
    runtime: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SECRET_KEY
        value: pricehunter-secret-2026
      - key: SCRAPE_API_KEY
        value: pricehunter-scrape-2026
      - key: EMAIL_SENDER
        sync: false
      - key: EMAIL_PASSWORD
        sync: false
      - key: EMAIL_RECEIVER
        sync: false
      - key: PYTHONPATH
        value: .
      - key: USE_PLAYWRIGHT
        value: "false"
    plan: free
    region: oregon
    healthCheckPath: /health
